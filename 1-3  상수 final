void main() {
/*변수는 1-1에서 한 것 같이 값이 의도치 않게 변할 수 있기 때문에 값이 변해버려서
  오류가 일어날 수 있는 사태를 방지하기 위해 변수를 선언할때는 값이 변하지 않는 상수로 사용하기 위해
  변수 앞에 final 키워드를 앞에 붙이면 값이 변하지 않는 상수로 사용가능하다
 */
  final String name = '홍길동';
  
  name = '이하빈';
  //final을 변수 앞에 사용했기 때문에 아래 name 이하빈에서 값이 변하지 않고 오류가 나오는 모습을 볼 수 있다
  
  final s = 'qwer';
  //이런식으로 간단히 변수 타입을 생략하고 짧게 코드를 작성할 수 있음
  
  
  }

