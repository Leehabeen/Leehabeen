void main() {
  //증감연산자는 값을 1씩 증가시키거나 감소시키는 연산자로 후위 연산과 전위 연산이 있습니다.
  var num = 0;
  
  print(num++); // ++이 num뒤에 붙어 있어서 나중에 계산하므로 0이 출력
  
  //num의 값이 1이 됨
  
  print(++num); //++이 num 앞에 붙어 있어서 먼저 ++을 계산하기 때문에 2가 출력된다.
  
  //num의 값이 2가 됨
  }

