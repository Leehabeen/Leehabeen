void main() {
 /* 비교 연산자 
  == : 같다
  != : 다르다
  >  : 더 크다 
  <  : 더 작다 
  >= : 크거나 같다
  <= : 작거나 같다
  */
  assert(2 == 2);
  assert(2 != 3);
  assert(3 > 2);
  assert(2 < 3);
  assert(3 >= 3);
  assert(2 <= 3);
  //aseert 함수는 실행되지 않지만 이 코드는 모두 결과가 (true)참이 나옵니다.
  }

