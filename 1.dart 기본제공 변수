
//dart가 제공하는 기본 변수 타입들

void main() {
  
int i = 10;
 //정수 
  
double j = 10.0;
 //실수(소숫점 포함)
  
  bool b = true;
  // 참거짓 판별 if같은 조건문에서 사용할 예정
  
  String name;
  //문자열 변수 선언
  
  name = '홍길동';
   //String(문자열)로 선언한 name에 '홍길동'이라는 값을 할당해줌
  }

